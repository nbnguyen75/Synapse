# Phase Gate — Synapse AI Service

A feature or capability is complete only when implementation, verification, automated checks, and evidence all exist.

Within a domain module, ship MVP capabilities before speculative enhancements (see `product.md`). Do not pull out-of-scope work into current tasks without explicit user instruction — refer to `feature_list.json` or active plan specifications.
